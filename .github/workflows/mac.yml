name: macOS

on: [push]

jobs:
  build:
    name: Build and test
    runs-on: macos-latest
    steps:
      - name: pre-commit
        run: bazelisk run pre-commit -- run --all-files
      - uses: actions/checkout@v2
      - name: build
        run: bazelisk build //...
      - name: test
        run: bazelisk test //...
